[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
lief = ">=0.9.0"
tqdm = ">=4.31.0"
numpy = ">=1.16.3"
pandas = ">=0.24.2"
lightgbm = ">=2.2.3"
scikit-learn = ">=0.20.3"
dask = {extras = ["complete"],version = "*"}
dask-ml = "*"
joblib = "*"
matplotlib = ">=3.1.1"
jupyter = ">=1.0.0"
vega = ">=2.5"
vega-datasets = ">=0.7.0"
altair = ">=3.1.0"

[requires]
python_version = "3.7"
